{
  "name": "mits-warden-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"cd server && .venv\\Scripts\\uvicorn app.main:app --reload\" \"cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9"
  }
}
